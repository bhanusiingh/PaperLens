# Notebooks

Jupyter notebooks for exploratory data analysis, dataset inspection,
and result visualization.

> **Note:** Notebooks are excluded from version control via `.gitignore`
> (`.ipynb` files). Export key findings as scripts or markdown reports
> before committing.
